# Decisions (ADR-derived)

> **Source policy:** ADR > SPEC > PRD > DOC. This file aggregates decisions extracted from ADR-class sources. Decisions inherited from SPECs (technology choices, architecture) are recorded in `constraints.md`; this file is reserved for explicit ADRs.

## Status

**No ADRs ingested in this synthesis.**

The ingest set contained 1 SPEC and 1 DOC; no decision records (`adr/*` or equivalent) were classified. All technology and architecture choices in this project flow from the design SPEC and are therefore recorded as constraints/context, not as locked decisions.

If formal ADRs are introduced later (e.g. to lock a hosting choice, datastore choice, or LLM provider), they should be ingested via `/gsd-ingest-docs` and will populate this file.

---

*End of decisions.md*
