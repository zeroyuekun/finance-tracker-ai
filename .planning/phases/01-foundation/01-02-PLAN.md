---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - postcss.config.mjs
  - .gitignore
  - .nvmrc
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - vitest.config.ts
  - tests/sanity.test.ts
  - components.json
  - components/ui/button.tsx
  - components/ui/card.tsx
  - components/ui/input.tsx
  - components/ui/label.tsx
  - lib/utils.ts
autonomous: true
requirements: [REQ-auth]

must_haves:
  truths:
    - "`npm run dev` starts a Turbopack dev server on http://localhost:3000 with the landing page rendering 'Finance Tracker AI' heading and a Get started Button"
    - "`npm run typecheck` exits 0 with strict + noUncheckedIndexedAccess + noImplicitOverride enabled"
    - "`npm test` runs Vitest and passes the sanity test (1+ tests passed)"
    - "shadcn/ui primitives Button, Card, Input, Label are importable from `@/components/ui/*`"
  artifacts:
    - path: package.json
      provides: "Next.js 16 + TypeScript + Tailwind v4 + Vitest + shadcn deps + scripts (dev, build, start, lint, typecheck, test, test:watch)"
      contains: "next"
    - path: tsconfig.json
      provides: "Strict TypeScript config"
      contains: "noUncheckedIndexedAccess"
    - path: vitest.config.ts
      provides: "Vitest runner configured with @/ alias and Node environment"
      contains: "defineConfig"
    - path: tests/sanity.test.ts
      provides: "Smoke test for the test framework"
      contains: "expect(2 + 2).toBe(4)"
    - path: components.json
      provides: "shadcn/ui config (New York, Neutral, CSS variables)"
      contains: "new-york"
    - path: components/ui/button.tsx
      provides: "shadcn Button primitive"
      exports: ["Button", "buttonVariants"]
    - path: components/ui/card.tsx
      provides: "shadcn Card primitive"
      exports: ["Card", "CardHeader", "CardTitle", "CardContent"]
    - path: components/ui/input.tsx
      provides: "shadcn Input primitive"
      exports: ["Input"]
    - path: components/ui/label.tsx
      provides: "shadcn Label primitive"
      exports: ["Label"]
    - path: lib/utils.ts
      provides: "shadcn cn helper (tailwind-merge + clsx)"
      exports: ["cn"]
    - path: app/page.tsx
      provides: "Landing page rendering shadcn Button"
      contains: "@/components/ui/button"
    - path: .nvmrc
      provides: "Pinned Node version for CI"
      contains: "20"
  key_links:
    - from: app/page.tsx
      to: components/ui/button.tsx
      via: "import { Button } from '@/components/ui/button'"
      pattern: "from\\s+\"@/components/ui/button\""
    - from: components/ui/button.tsx
      to: lib/utils.ts
      via: "import { cn } from '@/lib/utils'"
      pattern: "from\\s+\"@/lib/utils\""
    - from: tests/sanity.test.ts
      to: vitest
      via: "import { describe, expect, it } from 'vitest'"
      pattern: "from\\s+\"vitest\""
---

<objective>
Scaffold Next.js 16 + strict TypeScript + Tailwind v4 + Vitest + shadcn/ui primitives. Adapts Tasks 1, 2, 3 of the source implementation plan into three sequential tasks. Output: runnable scaffold with `npm run dev` showing landing page, `npm run typecheck` green, `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/PREREQS.md
@docs/superpowers/plans/2026-05-06-phase-1-foundation.md

<interfaces>
After this plan, downstream plans (03, 04) will import:
- From `@/components/ui/button`: `Button`, `buttonVariants` (Button supports `variant`, `size`, `asChild` props)
- From `@/components/ui/card`: `Card`, `CardHeader`, `CardTitle`, `CardContent` (plus `CardDescription`, `CardFooter`)
- From `@/components/ui/input`: `Input`
- From `@/components/ui/label`: `Label`
- From `@/lib/utils`: `cn(...inputs: ClassValue[]): string`

Scripts in package.json (downstream plans rely on these names exactly):
- `dev` (next dev --turbopack), `build` (next build), `start` (next start)
- `lint` (next lint OR eslint .), `typecheck` (tsc --noEmit)
- `test` (vitest run), `test:watch` (vitest)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run create-next-app, install deps, pin Node, write strict tsconfig.json</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, .gitignore, .nvmrc, app/layout.tsx, app/page.tsx, app/globals.css</files>
  <read_first>
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Task 1, steps 1–9)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions — Framework + language section)
    - .planning/phases/01-foundation/PREREQS.md (confirms Node 20+ available)
  </read_first>
  <action>
1. From project root, run: `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --use-npm --turbopack --skip-install`. Answer Yes if prompted to overwrite (the `.git`, `.planning/`, `docs/` are untouched).
2. Run `npm install` to populate `node_modules/`.
3. Create `.nvmrc` containing exactly `20` (single line).
4. Replace `tsconfig.json` so `compilerOptions` is exactly:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```
The three critical settings (locked in CONTEXT.md decisions) are `strict: true`, `noUncheckedIndexedAccess: true`, `noImplicitOverride: true`.
5. Run `npx tsc --noEmit` — must exit 0. If create-next-app's default code triggers strict errors, fix in place; do NOT relax tsconfig.
6. Verify `.gitignore` already covers env files: `Select-String -Path .gitignore -Pattern '\.env'` should match. If `.env*.local` is missing, append a new line `.env*.local` to `.gitignore`.
7. Sanity-check dev server: run `npm run dev` in background, wait 5s, `curl http://localhost:3000` (or `Invoke-WebRequest`) and confirm HTTP 200, then kill the dev process. Do NOT leave it running.
8. Stage and commit: `git add .` then `git commit -m "feat(01-02): initialize Next.js 16 with TypeScript, Tailwind, strict tsconfig, Node 20 pin"`. (If this is the repo's first commit, that's expected — `docs/` and `.planning/` get staged too.)
  </action>
  <verify>
    <automated>PowerShell: `Test-Path package.json` AND `Test-Path tsconfig.json` AND `Test-Path .nvmrc`; `(Get-Content .nvmrc -Raw).Trim() -eq '20'`; `Select-String -Path tsconfig.json -Pattern 'noUncheckedIndexedAccess' -Quiet`; `Select-String -Path tsconfig.json -Pattern 'noImplicitOverride' -Quiet`; `npx tsc --noEmit` exit 0; `Select-String -Path .gitignore -Pattern '\.env\*?\.local' -Quiet`</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at project root, contains `"next":` (any major 15+ acceptable as Next 16 may still be beta), `"typescript":`, `"tailwindcss":`
    - `tsconfig.json` contains the literal strings `"strict": true`, `"noUncheckedIndexedAccess": true`, `"noImplicitOverride": true`
    - `tsconfig.json` contains `"@/*"` in paths mapping
    - `.nvmrc` content (trimmed) equals exactly `20`
    - `.gitignore` contains a line matching the regex `\.env\*?\.local`
    - `app/page.tsx`, `app/layout.tsx`, `app/globals.css` all exist (defaults from create-next-app)
    - `node_modules/next/package.json` exists (proves `npm install` succeeded)
    - `npx tsc --noEmit` exits 0
    - `npm run dev` returned HTTP 200 on http://localhost:3000 within 10s, then was terminated cleanly
    - `git log --oneline -n 5` contains the commit subject "feat(01-02): initialize Next.js 16"
  </acceptance_criteria>
  <done>Next.js scaffold installed, strict tsconfig committed, dev server responsive, typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Add Vitest framework + sanity test + npm scripts</name>
  <files>package.json, vitest.config.ts, tests/sanity.test.ts, package-lock.json</files>
  <read_first>
    - package.json (current state from Task 1 — read to know exact scripts block format)
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Task 2, steps 1–6)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions — Tests section: Vitest with Node env, sanity + db + schema tests planned)
  </read_first>
  <action>
1. Install Vitest devDeps: `npm install -D vitest @vitest/ui @testing-library/react @testing-library/jest-dom jsdom @types/node`.
2. Create `vitest.config.ts` at project root with EXACTLY:
```ts
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  test: {
    environment: "node",
    globals: true,
    setupFiles: [],
    include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "."),
    },
  },
});
```
Node environment is correct per CONTEXT.md (sanity + db + schema tests are server-side).
3. Edit `package.json` `"scripts"` block to contain at minimum these keys (preserve any extras create-next-app added that don't conflict; preserve `--turbopack` flag on dev if create-next-app emitted it):
```json
"dev": "next dev --turbopack",
"build": "next build",
"start": "next start",
"lint": "next lint",
"typecheck": "tsc --noEmit",
"test": "vitest run",
"test:watch": "vitest"
```
Note: Next 16 may have removed `next lint` in favor of standalone ESLint. If `npm run lint` fails after this task, swap to `"lint": "eslint ."` in Task 3 and document in SUMMARY. Don't block on this here.
4. Create `tests/sanity.test.ts` (create the `tests/` directory if absent) with EXACTLY:
```ts
import { describe, expect, it } from "vitest";

describe("sanity", () => {
  it("runs vitest", () => {
    expect(2 + 2).toBe(4);
  });
});
```
5. Run `npm test` — must exit 0 with at least 1 passing test in output.
6. Stage and commit: `git add vitest.config.ts tests/sanity.test.ts package.json package-lock.json` then `git commit -m "feat(01-02): add Vitest test framework with sanity test + npm scripts"`.
  </action>
  <verify>
    <automated>PowerShell: `Test-Path vitest.config.ts` AND `Test-Path tests/sanity.test.ts`; `Select-String -Path package.json -Pattern '"vitest":' -Quiet`; `Select-String -Path package.json -Pattern '"test":\s*"vitest run"' -Quiet`; `Select-String -Path package.json -Pattern '"typecheck":\s*"tsc --noEmit"' -Quiet`; `npm test` exits 0 with output containing the substring "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `defineConfig` AND `environment: "node"` AND the alias `"@": path.resolve(__dirname, ".")`
    - `tests/sanity.test.ts` contains `expect(2 + 2).toBe(4)`
    - `package.json` `"scripts"` has all of: `"test": "vitest run"`, `"test:watch": "vitest"`, `"typecheck": "tsc --noEmit"`, `"lint":` (any value), `"dev":` (containing `next dev`)
    - `package.json` `"devDependencies"` contains `vitest`, `@vitest/ui`, `jsdom`, `@types/node`
    - `npm test` exits 0 and reports >= 1 passing test
    - `git log --oneline -n 5` contains "feat(01-02): add Vitest"
  </acceptance_criteria>
  <done>Vitest installed, sanity test green, npm scripts in place, committed.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize shadcn/ui, add Button/Card/Input/Label, render landing page</name>
  <files>components.json, components/ui/button.tsx, components/ui/card.tsx, components/ui/input.tsx, components/ui/label.tsx, lib/utils.ts, app/globals.css, app/page.tsx, package.json, package-lock.json</files>
  <read_first>
    - app/page.tsx (current state from Task 1 — will be overwritten)
    - app/globals.css (current state — shadcn init will modify with theme tokens)
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Task 3, steps 1–6)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions — UI: shadcn New York, Neutral, CSS variables; primitives Button, Card, Input, Label only)
  </read_first>
  <action>
1. Run shadcn init non-interactive: `npx shadcn@latest init --yes --base-color neutral --css-variables true`. If prompts appear despite flags, answer: Style = New York; Base color = Neutral; CSS variables = Yes (locked decisions). Expected: creates `components.json` with `"style": "new-york"`, `"baseColor": "neutral"`, `"cssVariables": true`; creates `lib/utils.ts` exporting `cn`; modifies `app/globals.css` with HSL theme tokens. New devDeps appear in package.json: `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`.
2. Add the four UI primitives: `npx shadcn@latest add button card input label`. Answer Yes to overwrite if any conflict. Expected: creates `components/ui/button.tsx`, `components/ui/card.tsx`, `components/ui/input.tsx`, `components/ui/label.tsx`.
3. Replace `app/page.tsx` with EXACTLY (this version exercises Button; Plan 04 Task 4 later replaces it with a `<Link href="/signin">` version):
```tsx
import { Button } from "@/components/ui/button";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-3xl font-semibold">Finance Tracker AI</h1>
      <p className="text-muted-foreground">Conversational, framework-grounded budgeting.</p>
      <Button>Get started</Button>
    </main>
  );
}
```
4. Verify dev render: `npm run dev` in background, wait 5s, `Invoke-WebRequest http://localhost:3000` and confirm response body contains both `Finance Tracker AI` AND `Get started`. Kill the dev process.
5. Run `npm run typecheck` — must exit 0.
6. Run `npm run lint` — must exit 0. If it fails because Next 16 removed `next lint`, edit `package.json` to set `"lint": "eslint ."` and re-run; document this swap in SUMMARY.
7. Stage and commit: `git add .` then `git commit -m "feat(01-02): add shadcn/ui (button, card, input, label) and landing page"`.
  </action>
  <verify>
    <automated>PowerShell: `Test-Path components.json` AND `Test-Path components/ui/button.tsx` AND `Test-Path components/ui/card.tsx` AND `Test-Path components/ui/input.tsx` AND `Test-Path components/ui/label.tsx` AND `Test-Path lib/utils.ts`; `Select-String -Path components.json -Pattern '"style":\s*"new-york"' -Quiet`; `Select-String -Path components.json -Pattern '"baseColor":\s*"neutral"' -Quiet`; `Select-String -Path app/page.tsx -Pattern '@/components/ui/button' -Quiet`; `Select-String -Path app/page.tsx -Pattern 'Finance Tracker AI' -Quiet`; `Select-String -Path app/page.tsx -Pattern 'Get started' -Quiet`; `npm run typecheck` exits 0; `npm run lint` exits 0; `npm test` exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists, contains `"style": "new-york"` AND `"baseColor": "neutral"` AND `"cssVariables": true`
    - All four primitives exist: `components/ui/button.tsx`, `components/ui/card.tsx`, `components/ui/input.tsx`, `components/ui/label.tsx`
    - `lib/utils.ts` exists and exports `cn` (file contains `export function cn` or `export const cn`)
    - `app/page.tsx` imports from `@/components/ui/button` and contains the literal strings "Finance Tracker AI" and "Get started"
    - `app/globals.css` contains theme variables (Tailwind v4 either uses `@theme` block or HSL CSS vars under `:root`)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0 (with either `next lint` or `eslint .` script)
    - `npm test` still exits 0 (sanity test unaffected)
    - Dev server returned HTTP 200 with body containing "Finance Tracker AI" and "Get started"
    - `git log --oneline -n 5` contains "feat(01-02): add shadcn/ui"
  </acceptance_criteria>
  <done>shadcn primitives wired, landing page renders, typecheck/lint/test green, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Packages downloaded; supply-chain trust delegated to Node ecosystem |
| Filesystem -> committed repo | Files staged via `git add .` could include accidental secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | npm dependencies | accept | Standard public packages from official registry; locked via `package-lock.json`. Renovate/dependabot is out of scope for Phase 1. |
| T-01-02-02 | Information Disclosure | accidental commit of secrets | mitigate | Task 1 step 6 verifies `.gitignore` matches `\.env\*?\.local` BEFORE any env file is created. `.env.local` is created in Plan 03 only after this gate is green. |
| T-01-02-03 | Tampering | shadcn registry | accept | Components copy-pasted from public registry into local `components/ui/`. Once vendored, future changes are local. |
| T-01-02-04 | Denial of Service | runaway dev server | mitigate | Each `npm run dev` invocation in this plan is started in background, sanity-checked with one HTTP request, then killed. No persistent dev server across tasks. |
</threat_model>

<verification>
- `npm run dev` returns HTTP 200 on http://localhost:3000 with body containing "Finance Tracker AI" and "Get started"
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm test` exits 0 with at least 1 passing test (sanity)
- `.gitignore` covers `.env*.local`
- Three commits exist with subjects starting "feat(01-02):"
</verification>

<success_criteria>
Plan 02 is complete when:
1. Scaffold installed (next, react, typescript, tailwind, eslint, vitest, shadcn primitives)
2. tsconfig.json has strict + noUncheckedIndexedAccess + noImplicitOverride
3. .nvmrc pins Node 20
4. .gitignore protects .env*.local
5. Landing page renders shadcn Button + heading
6. typecheck + lint + test all exit 0
7. Three commits made (one per task)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- Exact Next.js + React + Tailwind versions actually installed (read from package.json)
- Whether `lint` script ended up as `next lint` or `eslint .` (Next 16 fallback)
- Number of tests in suite (1 — sanity)
- Three commit SHAs
- Note: PREREQS.md still pristine; no `.env.local` created yet (deferred to Plan 03 Task 1)
</output>
