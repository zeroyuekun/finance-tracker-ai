---
phase: 01-foundation
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - .github/workflows/ci.yml
  - README.md
autonomous: false
requirements: [REQ-auth]

must_haves:
  truths:
    - "GitHub Actions workflow at .github/workflows/ci.yml runs lint -> typecheck -> test -> build on push to main and on every PR"
    - "All six repository secrets are set in GitHub: DATABASE_URL, AUTH_SECRET, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET, AUTH_RESEND_KEY, AUTH_EMAIL_FROM"
    - "Code is pushed to GitHub origin/main and the first CI run is GREEN"
    - "Vercel project is created from the GitHub repo with all 7 env vars wired (the 6 above + AUTH_TRUST_HOST=true) across Production, Preview, Development"
    - "Production URL of the form https://<project>.vercel.app responds with HTTP 200 to GET /"
    - "Production URL has been added to Google OAuth Authorized redirect URIs as https://<production-domain>/api/auth/callback/google"
    - "A real signup at the production URL via Google OAuth lands on /dashboard with name/email visible"
    - "A real magic-link signup works in production (email arrives, click lands on /dashboard)"
    - "A `user` row appears in production Neon Postgres after the first production sign-in"
    - "README.md contains a `**Live demo:** https://...` line linking to the Vercel production URL"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "CI pipeline (lint + typecheck + test + build)"
      contains: "actions/setup-node"
    - path: README.md
      provides: "Project description, stack, scripts, live demo URL"
      contains: "Live demo:"
  key_links:
    - from: .github/workflows/ci.yml
      to: GitHub Secrets
      via: "${{ secrets.DATABASE_URL }} etc."
      pattern: "secrets\\.DATABASE_URL"
    - from: README.md
      to: "Vercel production URL"
      via: "Live demo line"
      pattern: "Live demo"
    - from: "Google OAuth"
      to: "Vercel domain"
      via: "Authorized redirect URIs"
      pattern: "manual — verified by user reply"
---

<objective>
Ship Phase 1 to production. Set up GitHub Actions CI (lint + typecheck + test + build with secrets), push to GitHub, verify CI green, import the repo into Vercel with env vars wired, deploy, add production callback URL to Google OAuth, run end-to-end production signup verification, and update README with live demo URL.

Adapts source plan Tasks 8 and 9. This is the GO-LIVE plan and the final gate of Phase 1's Definition of Done. `autonomous: false` because it requires GitHub Settings UI work, Vercel UI work, Google Cloud Console work, and end-to-end production browser verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/PREREQS.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@docs/superpowers/plans/2026-05-06-phase-1-foundation.md

<interfaces>
GitHub Actions secrets schema (set in repo Settings -> Secrets and variables -> Actions):
- DATABASE_URL (string)
- AUTH_SECRET (string)
- AUTH_GOOGLE_ID (string)
- AUTH_GOOGLE_SECRET (string)
- AUTH_RESEND_KEY (string)
- AUTH_EMAIL_FROM (string)
(AUTH_TRUST_HOST is set as plain `"true"` env in workflow YAML, NOT a secret.)

Vercel project Environment Variables schema (Project Settings -> Environment Variables, all 3 environments):
- All 6 secrets above
- AUTH_TRUST_HOST=true (plaintext)

Google OAuth Authorized redirect URIs (must include both):
- http://localhost:3000/api/auth/callback/google (added in Plan 01 Task 4)
- https://<vercel-domain>/api/auth/callback/google (added in Task 4 of THIS plan, after Vercel domain is known)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CI workflow YAML and update README, push to GitHub</name>
  <files>.github/workflows/ci.yml, README.md</files>
  <read_first>
    - .planning/phases/01-foundation/PREREQS.md (GitHub repo URL from Plan 01 Task 2)
    - package.json (Plan 02 Task 2 — confirms scripts: lint, typecheck, test, build)
    - .nvmrc (Plan 02 Task 1 — Node version pin used by setup-node)
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Task 8, all steps)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions — CI/CD section)
  </read_first>
  <action>
1. Create `.github/workflows/ci.yml` (the directory `.github/workflows/` doesn't exist yet) with EXACTLY:
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    name: Lint · Typecheck · Test · Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Build
        run: npm run build
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
          AUTH_GOOGLE_ID: ${{ secrets.AUTH_GOOGLE_ID }}
          AUTH_GOOGLE_SECRET: ${{ secrets.AUTH_GOOGLE_SECRET }}
          AUTH_RESEND_KEY: ${{ secrets.AUTH_RESEND_KEY }}
          AUTH_EMAIL_FROM: ${{ secrets.AUTH_EMAIL_FROM }}
          AUTH_TRUST_HOST: "true"
```
Notes:
- `node-version-file: ".nvmrc"` reads the `20` from Plan 02 Task 1's `.nvmrc`.
- `cache: "npm"` caches `~/.npm` between runs for faster `npm ci`.
- `Test` step gets DATABASE_URL because tests/db.test.ts and tests/schema.test.ts hit live Neon.
- `Build` step gets full env because Next.js build evaluates server-side env at build time.
- `npm ci` (not `npm install`) for deterministic CI installs from `package-lock.json`.

2. Replace `README.md` content (the create-next-app default may exist) with EXACTLY:
````markdown
# Finance Tracker AI

A conversational AI advisor for personal finance — grounded in real CFP frameworks, not vibes.

## Stack

Next.js 16 · TypeScript · Tailwind · shadcn/ui · Drizzle ORM · Neon Postgres · Auth.js v5 · Vitest · Vercel

## Local development

```bash
# 1. Install dependencies
npm install

# 2. Copy env template and fill values
cp .env.example .env.local

# 3. Push schema to your Neon database
npm run db:push

# 4. Run dev server
npm run dev
```

## Scripts

- `npm run dev` — local dev server (Turbopack)
- `npm run build` — production build
- `npm test` — Vitest test suite
- `npm run lint` — ESLint
- `npm run typecheck` — TypeScript check
- `npm run db:generate` — generate migration from schema
- `npm run db:push` — push schema directly to DB

## Project status

Phase 1 (Foundation) — auth, DB, deployment.
````

(Live demo URL line added in Task 5 of this plan, after Vercel domain is known.)

3. Verify the GitHub remote. Run `git remote -v`. If empty, add origin from PREREQS.md:
```powershell
git remote add origin <URL from PREREQS.md row 2>
```
If the user provided a different URL via chat, prefer that. Confirm with `git remote -v` showing `origin` as fetch+push.

4. Commit:
```powershell
git add .github/workflows/ci.yml README.md
git commit -m "ci(01-05): add GitHub Actions workflow + project README"
```

5. Push the entire history to GitHub:
```powershell
git push -u origin main
```
First push uploads ALL commits accumulated across Plans 01–04 plus this one (since the repo was empty until now). Expected output: enumeration count, transferred bytes, branch tracking confirmation.

If push fails with auth error, the user may need to set up a Personal Access Token (HTTPS) or SSH key. PAUSE and surface the error verbatim — this is a credential issue not solvable in plan logic. The user should run `gh auth login` (if `gh` is installed) or set up Git Credential Manager.
  </action>
  <verify>
    <automated>PowerShell: `Test-Path .github/workflows/ci.yml`; `Select-String -Path .github/workflows/ci.yml -Pattern 'actions/checkout@v4' -Quiet`; `Select-String -Path .github/workflows/ci.yml -Pattern 'node-version-file:\s*\".nvmrc\"' -Quiet`; `Select-String -Path .github/workflows/ci.yml -Pattern 'npm ci' -Quiet`; `Select-String -Path .github/workflows/ci.yml -Pattern 'npm run lint' -Quiet`; `Select-String -Path .github/workflows/ci.yml -Pattern 'npm run typecheck' -Quiet`; `Select-String -Path .github/workflows/ci.yml -Pattern 'npm test' -Quiet`; `Select-String -Path .github/workflows/ci.yml -Pattern 'npm run build' -Quiet`; `Select-String -Path .github/workflows/ci.yml -Pattern 'secrets\.DATABASE_URL' -Quiet`; `Select-String -Path README.md -Pattern '^# Finance Tracker AI' -Quiet`; `Select-String -Path README.md -Pattern 'Next.js 16' -Quiet`; `Select-String -Path README.md -Pattern 'npm run db:push' -Quiet`; `git remote get-url origin` exits 0 and prints a github.com URL; `git ls-remote origin main` exits 0 (proves push succeeded — remote has the branch)</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists with: trigger `on: push branches main + pull_request`; uses `actions/checkout@v4` and `actions/setup-node@v4`; reads node from `.nvmrc`; runs all four steps lint/typecheck/test/build; injects DATABASE_URL into Test step; injects all 7 env vars into Build step (DATABASE_URL, AUTH_SECRET, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET, AUTH_RESEND_KEY, AUTH_EMAIL_FROM, AUTH_TRUST_HOST=true)
    - `README.md` has top-level `# Finance Tracker AI`, contains "Stack", "Local development", "Scripts" sections; contains `npm run db:push` reference
    - `git remote get-url origin` returns a `https://github.com/.../ai-finance-coach.git` URL
    - `git ls-remote origin main` returns a SHA (proves the branch is on the remote)
    - `git log origin/main --oneline | wc -l` is at least 5 (one commit per plan + Plan 05 Task 1)
  </acceptance_criteria>
  <done>CI YAML and README committed; entire history pushed to GitHub origin/main.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Add 6 repository secrets in GitHub Settings</name>
  <read_first>
    - .planning/phases/01-foundation/PREREQS.md (lists 6 secrets to set)
    - .github/workflows/ci.yml (Task 1 — confirms secret names referenced)
    - .env.local (current local values — same values get pasted as GitHub secrets)
  </read_first>
  <what-built>All 6 GitHub repository secrets configured so the just-pushed CI run can authenticate to Neon, Auth.js providers, and Resend during the Build step.</what-built>
  <how-to-verify>
**Manual steps (USER does these in browser):**

1. Open the GitHub repo's Settings page: `<repo URL>/settings/secrets/actions` (replace `<repo URL>` with the URL captured in Plan 01 Task 2 / PREREQS.md).
   - Direct path: github.com -> your repo -> Settings -> Secrets and variables -> Actions.

2. Click "New repository secret" and add each of the following (Name -> Secret value):
   - **DATABASE_URL** — paste the full Neon connection string from `.env.local`
   - **AUTH_SECRET** — paste the AUTH_SECRET value from `.env.local`
   - **AUTH_GOOGLE_ID** — paste the Google Client ID
   - **AUTH_GOOGLE_SECRET** — paste the Google Client Secret
   - **AUTH_RESEND_KEY** — paste the Resend API key (`re_...`)
   - **AUTH_EMAIL_FROM** — paste the from-address (e.g. `onboarding@resend.dev`)

3. After adding all 6, the page should show 6 entries. Click "Add secret" after each entry — there's no batch-import.

4. **DO NOT** add `AUTH_TRUST_HOST` as a secret — it's set as plain string `"true"` in the workflow YAML.

5. Reply "secrets added" once all 6 are visible in the list.
  </how-to-verify>
  <action>USER ACTION: Visit GitHub repo Settings -> Secrets and variables -> Actions. Add 6 secrets (DATABASE_URL, AUTH_SECRET, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET, AUTH_RESEND_KEY, AUTH_EMAIL_FROM) using the same values stored in `.env.local`. Reply "secrets added".

After user reply, Claude can re-trigger CI by re-running `git push` (if there's no commit, push --force is NOT needed; instead, an empty commit `git commit --allow-empty -m "ci(01-05): trigger CI rerun after secrets added"` followed by `git push` triggers a fresh run). Or, the user can re-run the failed workflow from the GitHub Actions UI.</action>
  <acceptance_criteria>
    - User reply contains "secrets added" or equivalent confirmation
    - User has confirmed they see exactly 6 secret entries in the GitHub Settings UI (DATABASE_URL, AUTH_SECRET, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET, AUTH_RESEND_KEY, AUTH_EMAIL_FROM)
    - User has NOT added AUTH_TRUST_HOST as a secret (plain text in YAML is intentional)
  </acceptance_criteria>
  <resume-signal>Reply "secrets added" with all 6 secret names listed</resume-signal>
  <verify>
    <automated>Manual checkpoint — verified by user reply. Cannot be automated since GitHub secret values are write-only after creation. Subsequent CI run (Task 3) IS the automated proof; if any secret is wrong, Build step fails.</automated>
  </verify>
  <done>User confirmed 6 secrets exist in GitHub repo Settings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify CI run is GREEN on main</name>
  <read_first>
    - .github/workflows/ci.yml (Task 1)
    - PREREQS.md (GitHub repo URL)
  </read_first>
  <what-built>The CI workflow has been triggered by the `git push` in Task 1, secrets were added in Task 2 — the run that ran BEFORE secrets were added likely failed at the Build step. We now need a fresh run with secrets in place, which must be GREEN before deploying to Vercel.</what-built>
  <how-to-verify>
**Pre-flight (Claude does these automated steps):**

1. Trigger a fresh CI run by pushing an empty commit (the prior run from Task 1's push will have failed at Build because secrets weren't yet set):
   ```powershell
   git commit --allow-empty -m "ci(01-05): trigger CI rerun after secrets added"
   git push
   ```
2. Wait for the run to start (check via `gh run list -L 1` if `gh` CLI is installed; otherwise poll the GitHub Actions UI).

**Manual verification (USER reports back):**

1. Open `<repo URL>/actions` in the browser.
2. Find the latest workflow run (top of the list, should be the empty-commit one Claude just pushed).
3. Wait for it to complete — typically 2–4 minutes for a fresh repo.
4. Expected: ALL FOUR jobs show green checkmarks (Lint · Typecheck · Test · Build is the single job name; the four steps inside it must all pass).
5. If a step fails, click into the run, read the error, and reply with the failing step name + error excerpt. Common failures:
   - `npm ci` fails -> package-lock.json out of date -> run `npm install` locally and push the updated `package-lock.json`
   - `Test` fails -> DATABASE_URL secret value is wrong or Neon DB not reachable -> re-paste DATABASE_URL secret in GitHub
   - `Build` fails with "AUTH_SECRET is required" or similar -> a Build env var is missing -> re-check secrets

**On success:** Reply "CI green".
**On failure:** Reply with the failing step + error excerpt; Claude will diagnose.
  </how-to-verify>
  <action>Claude runs `git commit --allow-empty -m "ci(01-05): trigger CI rerun after secrets added"; git push` to trigger a fresh CI run with the just-added secrets. Then surfaces this checkpoint asking the user to verify that the latest run on `<repo URL>/actions` is green. If `gh` CLI is available, Claude can poll: `gh run list --limit 1 --json status,conclusion` until status="completed" and conclusion="success". Otherwise the user reports back.</action>
  <acceptance_criteria>
    - User reply explicitly says "CI green" (or equivalent: "all jobs passed", "workflow successful")
    - OR if `gh` CLI is available: `gh run list --limit 1 --json conclusion --jq '.[0].conclusion'` returns the literal string "success"
    - The most recent commit on origin/main (the empty trigger commit) has been associated with a successful workflow run
  </acceptance_criteria>
  <resume-signal>Reply "CI green" or paste the failing step + error excerpt</resume-signal>
  <verify>
    <automated>If `gh` CLI installed: `gh run list --limit 1 --json conclusion --jq '.[0].conclusion'` returns "success". If not: Manual checkpoint, user reports back. Either path ends with the most recent run being green.</automated>
  </verify>
  <done>The latest CI run on main is green (all 4 steps: lint, typecheck, test, build).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Import repo into Vercel, set env vars, deploy, capture production URL, add Google OAuth production callback</name>
  <read_first>
    - PREREQS.md (Vercel + GitHub link confirmed in Plan 01 Task 5; Google OAuth client ID in Plan 01 Task 4)
    - .github/workflows/ci.yml (Task 1 — confirms env var names that need to mirror in Vercel)
    - .env.local (current local values — same get pasted into Vercel)
  </read_first>
  <what-built>Vercel project deployed to a production URL of the form `https://<project>.vercel.app`, with all 7 env vars set across Production/Preview/Development, and the production OAuth callback URL added to Google Cloud Console.</what-built>
  <how-to-verify>
**Manual steps (USER does these in browser):**

**Phase A — Vercel import:**

1. Visit https://vercel.com/new (logged in to the Vercel account from Plan 01 Task 5).
2. In "Import Git Repository" panel, locate `ai-finance-coach`. If it's not listed, click "Adjust GitHub App Permissions" and grant Vercel access to the repo.
3. Click "Import" next to the repo.
4. Framework Preset: should auto-detect "Next.js" (Vercel reads from `package.json`).
5. Root Directory: leave default (project root).
6. **Environment Variables (CRITICAL — DO NOT click Deploy yet):** Expand the "Environment Variables" panel. Add ALL 7 env vars. For each: enter Name, Value, and check ALL THREE environment scope checkboxes (Production, Preview, Development).
   - **DATABASE_URL** — same value as `.env.local`
   - **AUTH_SECRET** — same value as `.env.local`
   - **AUTH_GOOGLE_ID** — same value as `.env.local`
   - **AUTH_GOOGLE_SECRET** — same value as `.env.local`
   - **AUTH_RESEND_KEY** — same value as `.env.local`
   - **AUTH_EMAIL_FROM** — same value as `.env.local`
   - **AUTH_TRUST_HOST** — value: `true` (NOT `"true"` — Vercel UI accepts plain text)
7. Click "Deploy". Wait ~1–3 minutes for the build.
8. **On success:** Vercel shows a green checkmark and a URL like `https://ai-finance-coach-<random-suffix>.vercel.app`. Copy this URL.

**Phase B — Capture production domain:**

9. From the Vercel project dashboard, find the production domain. By default Vercel may assign multiple aliases (e.g. `ai-finance-coach.vercel.app` AND `ai-finance-coach-username.vercel.app`). Use the SHORTEST one (Vercel's "production" alias) for the OAuth callback. Copy the EXACT origin (no trailing slash).

**Phase C — Google OAuth production callback:**

10. Open https://console.cloud.google.com -> APIs & Services -> Credentials.
11. Click your OAuth 2.0 Client ID (the one created in Plan 01 Task 4).
12. In "Authorized redirect URIs", add a new entry:
    `https://<vercel-domain>/api/auth/callback/google`
    (Replace `<vercel-domain>` with the value captured in step 9. Example: `https://ai-finance-coach.vercel.app/api/auth/callback/google`.)
13. Click Save. Google may show "It can take 5 minutes to a few hours for settings to take effect" — this is normal. Usually it works in <1 minute for OAuth.

**Reply with:**
- The exact production URL (line: `Production URL: https://<vercel-domain>`)
- Confirmation: "Vercel deploy success, env vars set in all 3 environments, Google OAuth production callback added"
  </how-to-verify>
  <action>USER ACTION: Import repo into Vercel, set 7 env vars across all 3 environments, deploy, capture production URL, add `https://<vercel-domain>/api/auth/callback/google` to Google OAuth Authorized redirect URIs. Reply with the production URL and confirmation that all 3 phases (A, B, C) are complete.</action>
  <acceptance_criteria>
    - User reply contains a string matching the regex `https://[a-zA-Z0-9-]+\.vercel\.app` (the production URL)
    - User has confirmed Vercel deploy succeeded ("Ready" status, green checkmark)
    - User has confirmed all 7 env vars are set in all 3 Vercel environments
    - User has confirmed Google OAuth production callback URL is added (matches `https://<that-vercel-url>/api/auth/callback/google`)
    - User has NOT confused production with preview deployment URL (production URL is the canonical alias, typically shortest)
  </acceptance_criteria>
  <resume-signal>Reply with "Production URL: https://..." and confirmation of all 3 phases</resume-signal>
  <verify>
    <automated>Manual checkpoint. Subsequent Task 5 verification (HTTP 200 + production sign-in) is the automated proof of correctness.</automated>
  </verify>
  <done>Vercel project deployed, env vars wired, Google OAuth production redirect URI added, production URL captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: End-to-end production verification (URL responds, Google + magic-link sign-in work, user row in Neon)</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (specifics — Definition-of-Done checklist items 6–13)
    - app/(auth)/signin/page.tsx (Plan 04 Task 1)
    - app/(app)/dashboard/page.tsx (Plan 04 Task 1)
    - middleware.ts (Plan 03 Task 4)
  </read_first>
  <what-built>Phase 1 Definition of Done verified end-to-end against the live production URL: incognito visit shows landing, Google OAuth signup succeeds, magic-link signup succeeds, sign-out returns to landing, middleware redirects unauthenticated /dashboard, a user row appears in production Neon Postgres.</what-built>
  <how-to-verify>
**Pre-flight (Claude does these automated steps before pausing):**

1. Take the production URL from Task 4's user reply. Run:
   ```powershell
   $prod = "<production URL from Task 4>"
   Invoke-WebRequest -UseBasicParsing $prod
   ```
   Confirm HTTP 200 and the response body contains "Finance Tracker AI" + "Get started". If non-200, the deploy is broken — return to Task 4 and re-investigate Vercel build logs.

**Manual verification (USER does these in browser; report results back):**

In an incognito/private browser window, complete ALL of the following on the PRODUCTION URL:

1. **Landing renders:** Visit `<production URL>` (e.g. `https://ai-finance-coach.vercel.app`). Expected: same landing page as local (heading "Finance Tracker AI", subtitle, "Get started" button).

2. **Get started -> /signin:** Click "Get started". Expected: lands on `<production URL>/signin` showing the sign-in card.

3. **Middleware redirect (production):** Open new incognito tab. Visit `<production URL>/dashboard` directly (signed out). Expected: redirects to `<production URL>/signin?callbackUrl=%2Fdashboard`.

4. **Google OAuth (production):** From the signin page, click "Continue with Google". Expected: Google's OAuth consent screen appears. Sign in with your Google account. Expected: Google redirects back to `<production URL>/api/auth/callback/google`, which Auth.js handles, then lands you on `<production URL>/dashboard` with header "Welcome back, <your name or email>".

   If you see a Google error like "redirect_uri_mismatch", the production callback URL was not properly added to Google OAuth Authorized redirects in Task 4 step 12. Wait 2 minutes and retry, or re-check the URL exactly matches `<production URL>/api/auth/callback/google`.

5. **Sign out (production):** Click "Sign out" on the dashboard. Expected: returns to landing.

6. **Magic-link (production):** From `<production URL>/signin`, enter your real email, click "Send magic link". Expected: confirmation page. Within 1–2 minutes, an email arrives from `<AUTH_EMAIL_FROM>`. Click the link in the email. Expected: lands on `<production URL>/dashboard`.

   If email doesn't arrive after 2 minutes, check Spam folder. If still missing, check Resend dashboard for delivery logs.

7. **`user` row in PRODUCTION Neon:** Open Neon dashboard SQL editor. Run:
   ```sql
   SELECT id, email, name, "createdAt" FROM "user" ORDER BY "createdAt" DESC LIMIT 5;
   ```
   Expected: at least one row matching the email(s) from steps 4 and 6.

   This is the critical Definition-of-Done check (CONTEXT.md specifics item 12).

**Reply:** PASS/FAIL for each step 1–7. If all PASS, reply "Phase 1 Definition of Done verified" with the latest 1–2 user rows pasted (id + email + createdAt only, redact other fields).
  </how-to-verify>
  <action>Claude pre-flights with `Invoke-WebRequest <production URL>` to confirm HTTP 200 + correct body. Then surfaces this checkpoint with the 7-step production verification protocol. Claude waits for user reply. After "Phase 1 Definition of Done verified" with at least one production user row, Claude proceeds to Task 6 (README update with live URL).</action>
  <acceptance_criteria>
    - User reply confirms PASS for all 7 verification steps (or equivalent confirmation)
    - At least one row in production Neon `user` table (user pastes id + email + createdAt)
    - Production URL responds HTTP 200 (Claude's pre-flight automated)
    - User has not reported any redirect_uri_mismatch or other OAuth errors
    - Magic-link email arrived and link landed on /dashboard
  </acceptance_criteria>
  <resume-signal>Reply with PASS/FAIL for steps 1–7, plus pasted user row data</resume-signal>
  <verify>
    <automated>Pre-flight: `Invoke-WebRequest <production URL>` returns HTTP 200 with body containing "Finance Tracker AI" AND "Get started". OAuth flow itself can't be automated. User reply IS the gate marker.</automated>
  </verify>
  <done>All 7 production verification steps PASS. At least one user row in production Neon. Phase 1 Definition of Done items 6–13 verified.</done>
</task>

<task type="auto">
  <name>Task 6: Update README with live demo URL, commit, push</name>
  <files>README.md</files>
  <read_first>
    - README.md (Task 1 — current contents)
    - Production URL captured in Task 4 user reply
  </read_first>
  <action>
1. Read current `README.md` (created in Task 1 of this plan).
2. Insert a new line near the top, immediately after the `# Finance Tracker AI` heading. The line should be EXACTLY:
   ```
   **Live demo:** <production URL from Task 4>
   ```
   Example resulting top of README.md:
   ```
   # Finance Tracker AI

   **Live demo:** https://ai-finance-coach.vercel.app

   A conversational AI advisor for personal finance — grounded in real CFP frameworks, not vibes.
   ```

3. Stage and commit:
```powershell
git add README.md
git commit -m "docs(01-05): add live demo URL"
```

4. Push to GitHub:
```powershell
git push
```
This triggers another CI run (since push goes to main). Optionally wait for it to be green; not blocking — Phase 1 DoD is already verified end-to-end at this point.

5. Confirm Vercel auto-deploys this commit (Vercel Hobby auto-deploys main on every push). Visit Vercel dashboard -> Deployments -> latest should show "Ready" within 1–2 minutes. Not blocking.
  </action>
  <verify>
    <automated>PowerShell: `Select-String -Path README.md -Pattern '^\*\*Live demo:\*\*\s+https://' -Quiet`; `Select-String -Path README.md -Pattern 'vercel\.app' -Quiet`; `git log --oneline -n 3 origin/main` contains "docs(01-05): add live demo URL"; `git status` shows working tree clean</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains a line matching the regex `^\*\*Live demo:\*\*\s+https://[a-zA-Z0-9.-]+\.vercel\.app/?$`
    - The Live demo URL matches the production URL captured in Task 4
    - The commit `docs(01-05): add live demo URL` exists on origin/main (`git log origin/main --oneline -n 3` includes it)
    - `git status` shows working tree clean
  </acceptance_criteria>
  <done>README updated with live demo URL, committed, pushed. Phase 1 SHIPPED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo -> GitHub | Source code crosses to GitHub via `git push` |
| GitHub -> Actions runner -> Neon | CI runner authenticates via secrets and runs DB queries |
| GitHub -> Vercel | Vercel pulls source on every commit to main |
| Browser -> Vercel edge -> Auth.js handler | Production OAuth and magic-link flows |
| Auth.js handler -> Google / Resend (production) | Real OAuth code exchange, real email send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | secrets in CI logs | mitigate | GitHub Actions auto-redacts known secrets in log output. Workflow YAML never echoes env vars. Build step doesn't log env to stdout. |
| T-01-05-02 | Information Disclosure | secrets in Vercel logs | accept | Vercel redacts known env vars in build logs. Runtime logs (`/api/auth/...`) handle secrets server-side and don't print them. |
| T-01-05-03 | Tampering | unauthorized push to main | accept | GitHub default branch protection is off in Phase 1; user is the only collaborator. Phase 9 polish considers branch protection. |
| T-01-05-04 | Spoofing | OAuth callback redirect_uri_mismatch | mitigate | Task 4 step 12 requires the EXACT production callback URL to be whitelisted in Google OAuth before Task 5 verification. Task 5 step 4 catches the mismatch. |
| T-01-05-05 | Information Disclosure | accidental commit of .env.local | mitigate | Plan 02 Task 1 step 6 verified .gitignore covers `.env*.local`. `git ls-files` is checked in Plan 03 Task 4 acceptance. .env.local is NEVER staged in any plan's git add commands. |
| T-01-05-06 | Tampering | Vercel env var leakage to client bundle | mitigate | None of these env vars are prefixed with NEXT_PUBLIC_, so Next.js never inlines them into the client bundle. Verified by static analysis (no NEXT_PUBLIC_ usage in lib/auth.ts or lib/db). |
| T-01-05-07 | Denial of Service | CI minutes exhaustion | accept | GitHub Actions free tier: unlimited for public repos, 2000 min/mo for private. Public is recommended (PROJECT.md). |
| T-01-05-08 | Denial of Service | Vercel bandwidth | accept | Hobby tier 100 GB/mo bandwidth, ample for portfolio scale (<100 users). |
| T-01-05-09 | Repudiation | who deployed what when | mitigate | Vercel "Deployments" tab logs every deploy with commit SHA + author. README live-demo line links to current production. |
| T-01-05-10 | Elevation of Privilege | bypass production middleware | mitigate | Vercel runs middleware.ts at edge. Defense in depth: dashboard page also calls `auth()`. |
| T-01-05-11 | Information Disclosure | production magic-link email content | accept | Auth.js default template includes only the magic link, no PII. From-address `onboarding@resend.dev` is a Resend test domain — fine for v1. |
| T-01-05-12 | Tampering | force-push to main | accept | Out of scope: solo developer, no protected branches in Phase 1. |
</threat_model>

<verification>
- `.github/workflows/ci.yml` exists with all 4 steps and all 7 env vars
- `git ls-remote origin main` returns a SHA (proves push succeeded)
- Latest CI run on origin/main is green (verified manually OR via `gh run list`)
- 6 GitHub repository secrets exist (verified manually by user)
- Vercel project deployed; production URL responds HTTP 200
- All 7 env vars set in Vercel for Production/Preview/Development (verified manually)
- Google OAuth has production callback URL whitelisted (verified manually)
- 7-step production verification all PASS (manually)
- At least one user row in production Neon (manually + via SQL)
- README.md contains `**Live demo:**` line linking to production URL
- All Phase 1 Definition-of-Done items (CONTEXT.md specifics 1–13) satisfied:
  1. ✓ npm run lint passes locally (Plan 02 Task 3)
  2. ✓ npm run typecheck passes locally (Plan 04 Task 1)
  3. ✓ npm test passes locally with 3 tests (Plan 03 Task 3)
  4. ✓ npm run build passes locally (CI Build step is more rigorous and is green)
  5. ✓ GitHub Actions CI green on main (Task 3)
  6. ✓ Vercel production "Ready" (Task 4)
  7. ✓ Production URL incognito shows landing (Task 5 step 1)
  8. ✓ Google OAuth -> /dashboard with name/email (Task 5 step 4)
  9. ✓ Magic-link works in production (Task 5 step 6)
  10. ✓ Sign-out returns to landing (Task 5 step 5)
  11. ✓ Middleware redirects unauth /dashboard (Task 5 step 3)
  12. ✓ user row in production Neon (Task 5 step 7)
  13. ✓ README has live demo URL (Task 6)
</verification>

<success_criteria>
Plan 05 — and Phase 1 — is complete when:
1. CI workflow YAML created and pushed; CI runs green on main
2. All 6 repo secrets set in GitHub
3. Vercel project deployed; production URL captured; all 7 env vars wired across 3 environments
4. Google OAuth production callback URL whitelisted
5. Production landing/signin/dashboard/middleware all work in incognito
6. Production Google OAuth signup works end-to-end
7. Production magic-link signup works end-to-end
8. At least one user row in production Neon Postgres
9. README has live demo URL
10. All 13 Definition-of-Done items from CONTEXT.md verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` documenting:
- Final production URL (from user)
- GitHub repo URL (from PREREQS.md, now public/private as user chose)
- CI run URL of the first green run on main (from `gh run list` if available, or "see GitHub Actions tab")
- Vercel project URL
- Pasted user row count from production Neon (e.g. "1 user row, created 2026-05-06T...Z")
- Three Plan 05 commit SHAs: Task 1 (CI YAML + README), Task 3 (empty trigger commit), Task 6 (live demo URL)
- Confirmation: "Phase 1 Definition of Done satisfied: items 1–13 verified"
- Note: Phase 2 (Transactions) can now be planned via `/gsd-plan-phase 2`
</output>
