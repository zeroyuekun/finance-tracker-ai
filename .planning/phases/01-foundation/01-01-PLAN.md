---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [REQ-auth]
user_setup:
  - service: node
    why: "Required runtime for Next.js dev/build/test"
    env_vars: []
    dashboard_config:
      - task: "Install Node.js 20 LTS"
        location: "https://nodejs.org"
  - service: github
    why: "Source repo + Actions CI host"
    env_vars: []
    dashboard_config:
      - task: "Create empty GitHub repo (no README/license/.gitignore) named ai-finance-coach"
        location: "https://github.com/new"
      - task: "Capture HTTPS or SSH remote URL"
        location: "GitHub repo Settings -> repo URL"
  - service: neon
    why: "Postgres database (free tier serverless)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Dashboard -> Project -> Connection string (HTTP/Pooled, sslmode=require)"
    dashboard_config:
      - task: "Create Neon account and a new project"
        location: "https://neon.tech"
      - task: "Copy the connection string from the project dashboard (must include sslmode=require)"
        location: "Neon Dashboard -> Project -> Connection Details"
  - service: google-oauth
    why: "Google sign-in provider"
    env_vars:
      - name: AUTH_GOOGLE_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client ID"
      - name: AUTH_GOOGLE_SECRET
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client secret"
    dashboard_config:
      - task: "Create OAuth 2.0 Web Application Client ID"
        location: "https://console.cloud.google.com -> APIs & Services -> Credentials -> Create Credentials -> OAuth client ID -> Web application"
      - task: "Add authorized redirect URI: http://localhost:3000/api/auth/callback/google (production URI added later in Plan 05)"
        location: "OAuth client -> Authorized redirect URIs"
  - service: vercel
    why: "Hosting + auto-deploy from GitHub"
    env_vars: []
    dashboard_config:
      - task: "Create Vercel account and link your GitHub identity"
        location: "https://vercel.com/signup"
  - service: resend
    why: "Magic-link email provider for Auth.js Resend provider"
    env_vars:
      - name: AUTH_RESEND_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
      - name: AUTH_EMAIL_FROM
        source: "Resend Dashboard -> Domains (use onboarding@resend.dev for testing)"
    dashboard_config:
      - task: "Sign up for Resend free tier"
        location: "https://resend.com"
      - task: "Create an API key (full access)"
        location: "Resend Dashboard -> API Keys"
      - task: "Note your verified sender (default: onboarding@resend.dev)"
        location: "Resend Dashboard -> Domains"

must_haves:
  truths:
    - "Node 20+ is installed locally and detectable on PATH"
    - "An empty GitHub repo exists with origin remote available to push to"
    - "A Neon project exists and DATABASE_URL is captured"
    - "Google OAuth Client ID + Secret are captured with localhost redirect registered"
    - "A Vercel account is linked to GitHub for auto-deploy"
    - "A Resend API key + verified sender are captured"
  artifacts:
    - path: ".planning/phases/01-foundation/PREREQS.md"
      provides: "Recorded prerequisite captures (URL of GitHub repo, Neon hostname, presence-only flags for secrets — never the secret values themselves)"
      contains: "GitHub repo URL"
  key_links:
    - from: ".planning/phases/01-foundation/PREREQS.md"
      to: "execute-phase wave 2 onward"
      via: "All checkpoints in this plan must be marked completed by user before any subsequent plan runs"
      pattern: "all checkpoints approved"
---

<objective>
Capture the six external account / runtime prerequisites that the Phase 1 implementation plan calls "P1–P6" and that CONTEXT.md flags as `autonomous: false` manual setup. Without these, no later plan can run — Drizzle has no DATABASE_URL, Auth.js has no Google client, Resend has no API key, Vercel has no GitHub repo to import, and CI has no place to push secrets to.

Purpose: Front-load all human-required external work so that Plans 02–05 can run autonomously. Each step is a checkpoint that pauses execution until the user confirms completion and pastes the captured value.

Output: A confirmed set of credentials and account URLs, recorded in `.planning/phases/01-foundation/PREREQS.md` (presence-only — the actual secrets live in `.env.local` later, never committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@docs/superpowers/plans/2026-05-06-phase-1-foundation.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify Node.js 20+ is installed</name>
  <read_first>
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Prerequisites section, P1)
    - .planning/phases/01-foundation/01-CONTEXT.md (specifics — manual prerequisites)
  </read_first>
  <what-built>Confirmation that the local Node runtime is 20.x or 22.x (LTS).</what-built>
  <how-to-verify>
    1. Run `node --version` in PowerShell.
    2. Expected output: `v20.x.x` or `v22.x.x`.
    3. If output shows `v18.x.x` or older, install Node 20 LTS from https://nodejs.org and re-verify.
    4. Paste the version string back into the conversation as confirmation.
  </how-to-verify>
  <action>USER ACTION: Run `node --version` in a fresh PowerShell window. Confirm the version is at least v20.0.0. If older, install Node 20 LTS from https://nodejs.org/en/download (LTS installer for Windows x64). Reply with the version string when done.</action>
  <acceptance_criteria>
    - User reply contains a version string of the form `v20.x.x` or `v22.x.x` (regex: `^v(20|22)\.\d+\.\d+$` after trimming)
    - User has confirmed in chat: "node --version OK"
  </acceptance_criteria>
  <resume-signal>Reply with the `node --version` output (e.g. "v20.18.1") or "approved"</resume-signal>
  <verify>
    <automated>Manual checkpoint — verified by user reply containing version string</automated>
  </verify>
  <done>User has confirmed Node 20+ is on PATH and pasted the version string back into the conversation.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create GitHub repo and capture origin URL</name>
  <read_first>
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Prerequisites section, P2)
    - .planning/phases/01-foundation/01-CONTEXT.md (specifics — manual prerequisites)
  </read_first>
  <what-built>An empty GitHub repository, named `ai-finance-coach` (or similar), suitable for `git remote add origin <url>` plus `git push -u origin main`. The repo MUST be empty (no README, no .gitignore, no license) — the local repo already has these files.</what-built>
  <how-to-verify>
    1. Visit https://github.com/new in a browser logged into the GitHub account that will own the project.
    2. Repository name: `ai-finance-coach` (suggested) — any name works, just record it.
    3. Visibility: Public (recommended for portfolio + free Actions minutes) or Private.
    4. Do NOT check "Add a README file", "Add .gitignore", or "Choose a license" — leave the repo empty.
    5. Click "Create repository".
    6. Copy the HTTPS clone URL from the "Quick setup" panel (looks like `https://github.com/<user>/ai-finance-coach.git`).
    7. Paste that URL back into the conversation.
  </how-to-verify>
  <action>USER ACTION: Visit https://github.com/new, create an empty repo named `ai-finance-coach` (Public recommended). Do NOT initialize with README/license/.gitignore. After creating, copy the HTTPS clone URL (format: `https://github.com/<user>/ai-finance-coach.git`) and paste it back here.</action>
  <acceptance_criteria>
    - User reply contains a string matching the regex `^https://github\.com/[^/]+/[^/]+\.git$`
    - User has confirmed: "GitHub repo created, no README initialized"
    - The URL will later be written to `.planning/phases/01-foundation/PREREQS.md` and used by Plan 05 Task 1 (`git remote add origin <url>`)
  </acceptance_criteria>
  <resume-signal>Reply with the HTTPS clone URL of the new repo</resume-signal>
  <verify>
    <automated>Manual checkpoint — verified by user reply containing repo URL</automated>
  </verify>
  <done>User has provided the empty repo's HTTPS clone URL. The repo is empty (no commits yet).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Create Neon project and capture DATABASE_URL</name>
  <read_first>
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Prerequisites section, P3; Task 4 step 3 for env var format)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions — Database section)
  </read_first>
  <what-built>A Neon project with a connection string usable by `@neondatabase/serverless` HTTP driver and `drizzle-kit push`.</what-built>
  <how-to-verify>
    1. Sign up at https://neon.tech (free tier, no credit card required).
    2. Create a new project. Default region/branch is fine.
    3. From the project dashboard, locate "Connection Details" or "Connection String".
    4. Copy the **pooled** connection string. It MUST look like: `postgresql://<user>:<password>@<host>.neon.tech/<dbname>?sslmode=require`
    5. The hostname will look like `ep-cool-name-12345-pooler.us-east-2.aws.neon.tech` (region varies).
    6. Paste the connection string back into the conversation. It will be saved to `.env.local` (gitignored) in Plan 03 Task 1.
  </how-to-verify>
  <action>USER ACTION: Sign up at https://neon.tech, create a project, copy the pooled connection string from "Connection Details". Format must be: `postgresql://USER:PASS@HOST.neon.tech/DB?sslmode=require`. Paste the full string back. (It will not be committed — it goes into `.env.local` which is gitignored.)</action>
  <acceptance_criteria>
    - User reply contains a string matching the regex `^postgresql://[^:]+:[^@]+@[^/]+\.neon\.tech/[^?]+\?.*sslmode=require`
    - The hostname ends in `.neon.tech`
    - The connection string contains `sslmode=require` (mandatory for Neon HTTP driver)
    - User has confirmed: "Neon project created, DATABASE_URL captured"
  </acceptance_criteria>
  <resume-signal>Reply with the full Neon DATABASE_URL (format: `postgresql://...sslmode=require`)</resume-signal>
  <verify>
    <automated>Manual checkpoint — verified by user reply containing valid Neon URL pattern</automated>
  </verify>
  <done>User has provided a working Neon connection string ending in `.neon.tech/<db>?sslmode=require`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Create Google OAuth credentials and capture client ID + secret</name>
  <read_first>
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Prerequisites section, P4; Task 6 step 3 for env var format)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions — Auth section)
  </read_first>
  <what-built>A Google Cloud OAuth 2.0 Web Application Client ID with the localhost callback URL whitelisted. Production callback URL is added in Plan 05 Task 4 once the Vercel domain is known.</what-built>
  <how-to-verify>
    1. Visit https://console.cloud.google.com.
    2. Create a new project (or select an existing one) — name: `ai-finance-coach` (any name works).
    3. Navigate: APIs & Services -> OAuth consent screen -> configure (External, fill required fields, add yourself as a test user).
    4. Navigate: APIs & Services -> Credentials -> Create Credentials -> OAuth client ID.
    5. Application type: **Web application**.
    6. Authorized redirect URIs — add: `http://localhost:3000/api/auth/callback/google`
    7. Click "Create". A modal shows "Client ID" (ends in `.apps.googleusercontent.com`) and "Client secret" (starts with `GOCSPX-`).
    8. Copy both values. Paste BOTH back into the conversation, clearly labeled.
  </how-to-verify>
  <action>USER ACTION: At https://console.cloud.google.com, create OAuth 2.0 Client ID for a Web application. Authorized redirect URI: `http://localhost:3000/api/auth/callback/google` (production URL added later — that's Plan 05 Task 4). Reply with both:
    - AUTH_GOOGLE_ID=<the Client ID, ends in .apps.googleusercontent.com>
    - AUTH_GOOGLE_SECRET=<the Client secret, starts with GOCSPX->
  </action>
  <acceptance_criteria>
    - User reply contains a Client ID matching regex `[0-9]+-[a-z0-9]+\.apps\.googleusercontent\.com`
    - User reply contains a Client secret matching regex `^GOCSPX-[A-Za-z0-9_-]+`
    - User has confirmed: "Localhost redirect URI registered: http://localhost:3000/api/auth/callback/google"
  </acceptance_criteria>
  <resume-signal>Reply with `AUTH_GOOGLE_ID=...` and `AUTH_GOOGLE_SECRET=...` on separate lines</resume-signal>
  <verify>
    <automated>Manual checkpoint — verified by user reply containing both ID and secret in expected formats</automated>
  </verify>
  <done>User has provided AUTH_GOOGLE_ID (ends in `.apps.googleusercontent.com`) and AUTH_GOOGLE_SECRET (starts with `GOCSPX-`), and confirmed localhost redirect is registered.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Create Vercel account linked to GitHub</name>
  <read_first>
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Prerequisites section, P5; Task 9 for deployment flow)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions — Deployment section)
  </read_first>
  <what-built>A Vercel account with GitHub login linked, so that "Import Git Repository" in Plan 05 Task 5 can list the `ai-finance-coach` repo created in Task 2.</what-built>
  <how-to-verify>
    1. Visit https://vercel.com/signup.
    2. Choose "Continue with GitHub" — sign in with the SAME GitHub account that owns the repo from Task 2.
    3. After signup, Vercel will request access to your repos. Grant access (either all repos or just `ai-finance-coach`).
    4. Confirm in chat: "Vercel account exists, GitHub linked, repo visible from Vercel import dialog".
  </how-to-verify>
  <action>USER ACTION: Sign up at https://vercel.com using "Continue with GitHub" (same GitHub identity as Task 2). Grant Vercel repo access (at minimum to `ai-finance-coach`). Reply with confirmation: "Vercel + GitHub linked".</action>
  <acceptance_criteria>
    - User reply contains "Vercel" and "linked" (or equivalent confirmation phrase)
    - User confirms repo `ai-finance-coach` is visible when they go to https://vercel.com/new (import dialog)
  </acceptance_criteria>
  <resume-signal>Reply "Vercel linked" or describe issues</resume-signal>
  <verify>
    <automated>Manual checkpoint — verified by user confirmation</automated>
  </verify>
  <done>User has confirmed Vercel account exists and is linked to GitHub.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Create Resend account and capture API key + sender</name>
  <read_first>
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Prerequisites section, P6; Task 6 step 3 for env var format)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions — Auth section, Resend magic-link)
  </read_first>
  <what-built>A Resend API key and a verified sender address for Auth.js's Resend provider (used in `lib/auth.ts` Plan 03 Task 4).</what-built>
  <how-to-verify>
    1. Sign up at https://resend.com (free tier — 100 emails/day, no credit card).
    2. Navigate to API Keys -> Create API Key. Permission: "Sending access". Name: `ai-finance-coach-dev`.
    3. Copy the API key — it starts with `re_` and is shown ONCE only. Save somewhere safe.
    4. For the verified sender, you can use Resend's test domain: `onboarding@resend.dev` (already verified, works out-of-box).
    5. Paste both values back into the conversation:
       - AUTH_RESEND_KEY=<the API key, starts with re_>
       - AUTH_EMAIL_FROM=<the from address, e.g. onboarding@resend.dev>
  </how-to-verify>
  <action>USER ACTION: Sign up at https://resend.com. Create an API key (Sending access). For testing, use the verified sender `onboarding@resend.dev` (no DNS setup needed). Reply with:
    - AUTH_RESEND_KEY=<starts with re_>
    - AUTH_EMAIL_FROM=onboarding@resend.dev (or your verified sender)
  </action>
  <acceptance_criteria>
    - User reply contains a key matching regex `^re_[A-Za-z0-9_]+`
    - User reply contains an AUTH_EMAIL_FROM that is a valid email address (regex: `^[^@\s]+@[^@\s]+\.[^@\s]+$`)
    - The from address is either `onboarding@resend.dev` OR a domain the user has verified in Resend's dashboard
  </acceptance_criteria>
  <resume-signal>Reply with `AUTH_RESEND_KEY=re_...` and `AUTH_EMAIL_FROM=...` on separate lines</resume-signal>
  <verify>
    <automated>Manual checkpoint — verified by user reply containing key and email</automated>
  </verify>
  <done>User has provided AUTH_RESEND_KEY (starts with `re_`) and AUTH_EMAIL_FROM (valid email, verified in Resend or `onboarding@resend.dev`).</done>
</task>

<task type="auto">
  <name>Task 7: Record prerequisites in PREREQS.md (presence-only, no secrets)</name>
  <files>.planning/phases/01-foundation/PREREQS.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (specifics — manual prerequisites checklist)
    - docs/superpowers/plans/2026-05-06-phase-1-foundation.md (Prerequisites section)
  </read_first>
  <action>Create `.planning/phases/01-foundation/PREREQS.md` documenting that all six prerequisites have been captured. Record ONLY presence-flags and non-sensitive metadata — NEVER write the actual secret values. The actual secrets go into `.env.local` in Plan 03 Task 1 (which is gitignored).

Use this exact template, filling in only the user-provided GitHub URL and Neon hostname (everything else is presence-only):

```markdown
# Phase 1 Prerequisites — Captured

**Captured:** {today's date in YYYY-MM-DD}

## Status

| # | Prerequisite                                | Captured | Source / Notes |
|---|---------------------------------------------|----------|----------------|
| 1 | Node.js 20+ on PATH                         | YES      | Confirmed via `node --version` |
| 2 | GitHub repo created (empty, no README)      | YES      | URL: {paste user-provided HTTPS clone URL} |
| 3 | Neon project + DATABASE_URL                 | YES      | Hostname: {extract `.neon.tech` host from URL — e.g. ep-xxx-12345.us-east-2.aws.neon.tech}; full URL stored in `.env.local` (gitignored) |
| 4 | Google OAuth Client ID + Secret             | YES      | Localhost redirect registered: http://localhost:3000/api/auth/callback/google. Production redirect added in Plan 05 Task 4. |
| 5 | Vercel account linked to GitHub             | YES      | Will import repo at https://vercel.com/new in Plan 05 Task 5 |
| 6 | Resend API key + verified sender            | YES      | Sender (non-secret): {paste user-provided AUTH_EMAIL_FROM} |

## Secrets to inject in Plan 03

The following env vars will be written to `.env.local` (gitignored) by Plan 03 Task 1:
- DATABASE_URL
- AUTH_SECRET (generated in Plan 03 Task 4 via `npx auth secret`)
- AUTH_TRUST_HOST=true
- AUTH_GOOGLE_ID
- AUTH_GOOGLE_SECRET
- AUTH_RESEND_KEY
- AUTH_EMAIL_FROM

## Secrets to inject in Plan 05 (CI + Vercel)

Same six secrets above (minus `AUTH_TRUST_HOST` for Vercel, which is set to `true` directly in Vercel env config) get pasted into:
- GitHub Settings -> Secrets and variables -> Actions (Plan 05 Task 4)
- Vercel project Environment Variables, all environments (Plan 05 Task 5)

## Verification

All six prerequisites have been confirmed by the user via interactive checkpoints in this plan. No further blocker remains for Plan 02 (project scaffold) to run autonomously.
```

After writing the file, commit with: `git add .planning/phases/01-foundation/PREREQS.md && git commit -m "chore(01-01): record Phase 1 prerequisite captures (presence-only)"` — but only if a `.git` directory exists at the project root. If no `.git` directory exists yet (it should — `git status` will tell you), skip the commit and note it in the summary; Plan 02 Task 1 (`create-next-app`) will not affect this file because it lives under `.planning/`.</action>
  <verify>
    <automated>Test-D PowerShell: `Test-Path .planning/phases/01-foundation/PREREQS.md` returns True; file contains 6 occurrences of `YES` in the status table; file does NOT contain any string matching `re_[A-Za-z0-9]{10,}` or `GOCSPX-` or `postgresql://[^@]+@` (no secret leaked)</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation/PREREQS.md` exists
    - File contains the literal string "Phase 1 Prerequisites — Captured"
    - File contains the GitHub repo URL provided by user in Task 2 (matches `https://github.com/`)
    - File contains the AUTH_EMAIL_FROM value provided in Task 6 (matches `@`)
    - File contains exactly 6 rows in the status table, all marked `YES`
    - File does NOT contain any actual API key, OAuth secret, or full DATABASE_URL password (grep for `re_[A-Za-z0-9_]{20,}` returns 0; grep for `GOCSPX-` returns 0; grep for `postgresql://[^/@\s]*:[^@\s]+@` returns 0)
  </acceptance_criteria>
  <done>PREREQS.md committed (or noted for commit in Plan 02), all six rows marked YES, no secrets leaked into the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> external dashboards | User authenticates with third-party providers (GitHub, Neon, Google Cloud, Vercel, Resend) outside our control |
| External secrets -> local `.env.local` | API keys/OAuth secrets cross from external dashboard UI into local file (must be gitignored) |
| External secrets -> chat | Secrets are pasted into the conversation as part of checkpoint completion (transient, not persisted by GSD) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | PREREQS.md | mitigate | Task 7 acceptance criteria explicitly forbids writing secret values; only presence-flags and non-sensitive metadata (GitHub URL, Neon hostname only, AUTH_EMAIL_FROM which is a public address) are persisted. Automated grep gate enforces this. |
| T-01-01-02 | Information Disclosure | `.env.local` | mitigate | Plan 03 Task 1 creates `.env.local` AFTER `.gitignore` already lists `.env*.local` (Next.js default + Task 1 step 4). Verified with `git check-ignore`. |
| T-01-01-03 | Spoofing | Google OAuth client | accept | Localhost redirect URI is the only one registered until Plan 05 Task 4 — limits attack surface to local dev. Production URI added after Vercel domain is known. |
| T-01-01-04 | Tampering | GitHub repo | accept | User-controlled GitHub repo, standard GitHub auth (2FA encouraged but out of GSD scope). |
| T-01-01-05 | Repudiation | Manual checkpoints | accept | All six checkpoints log user replies in conversation history. PREREQS.md records confirmation timestamps. |
| T-01-01-06 | Denial of Service | Resend free tier | accept | 100 emails/day cap is more than enough for personal dev + portfolio scale (<100 users in v1). |
| T-01-01-07 | Elevation of Privilege | Resend API key | mitigate | Key has "Sending access" only (not "Full access") per Task 6 instructions. |
</threat_model>

<verification>
- All six checkpoint tasks (1–6) have been marked completed by user
- `.planning/phases/01-foundation/PREREQS.md` exists with all six rows marked YES
- No secret values are present in PREREQS.md (grep gate enforced in Task 7 verify)
- User has captured (in `.env.local` later or somewhere safe NOW) all of: DATABASE_URL, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET, AUTH_RESEND_KEY, AUTH_EMAIL_FROM, plus the GitHub repo URL
</verification>

<success_criteria>
Plan 01 is complete when:
1. User has confirmed Node 20+ via checkpoint reply
2. User has provided GitHub repo HTTPS clone URL via checkpoint reply
3. User has provided full Neon DATABASE_URL via checkpoint reply
4. User has provided AUTH_GOOGLE_ID + AUTH_GOOGLE_SECRET via checkpoint reply
5. User has confirmed Vercel + GitHub link via checkpoint reply
6. User has provided AUTH_RESEND_KEY + AUTH_EMAIL_FROM via checkpoint reply
7. `.planning/phases/01-foundation/PREREQS.md` exists, has all six rows marked YES, contains no secret values
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Each prerequisite captured (status: YES)
- The GitHub repo URL (non-secret)
- The Neon hostname only (non-secret)
- AUTH_EMAIL_FROM (non-secret)
- Explicit note: "Secrets DATABASE_URL / AUTH_GOOGLE_ID / AUTH_GOOGLE_SECRET / AUTH_RESEND_KEY are held by user and will be pasted into `.env.local` in Plan 02 Task 1 / Plan 03 Task 1 — never written to any committed file."
- Confirmation that no later plan is blocked
</output>
